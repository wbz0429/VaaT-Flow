# 本地开发启动指南

本地开发不需要服务器。两人在各自电脑上跑现有的 `make dev` 开发环境，在这个基础上改代码。

---

## 本地环境准备

你们现在的开发环境（`make dev` 能跑通）已经够用。额外需要装两个东西：

### PostgreSQL（本地）

```bash
# macOS
brew install postgresql@16
brew services start postgresql@16

# 创建数据库
createdb allo
psql -d allo -c "CREATE EXTENSION IF NOT EXISTS \"uuid-ossp\";"
psql -d allo -c "CREATE EXTENSION IF NOT EXISTS \"pgcrypto\";"
```

### Redis（本地）

```bash
# macOS
brew install redis
brew services start redis
```

### 验证

```bash
psql -d allo -c "SELECT 1"       # PostgreSQL 正常
redis-cli ping                    # Redis 正常 → PONG
```

### 环境变量

在项目根目录 `.env` 中追加：

```
DATABASE_URL=postgresql+asyncpg://localhost:5432/allo
REDIS_URL=redis://localhost:6379/0
CHECKPOINT_POSTGRES_URI=postgresql://localhost:5432/allo
SESSION_SECRET=local-dev-secret-change-in-prod
API_KEY_ENCRYPTION_SECRET=local-dev-key-change-in-prod
```

---

## 开发分支

```bash
git checkout -b feat/multi-tenant
```

两人都在这个分支上工作。A 改 `backend/app/gateway/` 和 `frontend/`，B 改 `backend/packages/harness/deerflow/`，冲突面很小。

---

## 第一步：接口契约（两人一起，半天）

在写任何业务代码之前，先把两人之间的接口定义成代码文件提交。这是最重要的一步。

### 任务 0.1：抽象接口文件（B 写，A 确认）

新建 `backend/packages/harness/deerflow/stores.py`：

```python
"""
跨层抽象接口。
harness 层定义接口，app 层提供实现。
harness 不 import app 层代码。
"""
from abc import ABC, abstractmethod


class MemoryStore(ABC):
    @abstractmethod
    async def get_memory(self, user_id: str) -> dict: ...

    @abstractmethod
    async def save_memory(self, user_id: str, data: dict) -> None: ...

    @abstractmethod
    async def get_facts(self, user_id: str, limit: int = 15) -> list[dict]: ...


class SoulStore(ABC):
    @abstractmethod
    async def get_soul(self, user_id: str) -> str | None: ...


class SkillConfigStore(ABC):
    @abstractmethod
    async def get_skill_toggles(self, user_id: str) -> dict[str, bool]: ...


class McpConfigStore(ABC):
    @abstractmethod
    async def get_user_mcp_config(self, user_id: str) -> dict: ...


class ModelKeyResolver(ABC):
    @abstractmethod
    async def resolve_key(self, run_id: str) -> tuple[str, str | None]: ...
    # returns (api_key, base_url | None)
```

### 任务 0.2：UserContext 文件（B 写）

新建 `backend/packages/harness/deerflow/context.py`：

```python
from dataclasses import dataclass


@dataclass(frozen=True)
class UserContext:
    user_id: str
    org_id: str
    run_id: str | None = None


def get_user_context(config: dict | None = None) -> UserContext | None:
    """从 RunnableConfig 提取用户上下文。"""
    if not config:
        return None
    configurable = config.get("configurable", {})
    user_id = configurable.get("x-user-id") or configurable.get("user_id")
    org_id = configurable.get("x-org-id") or configurable.get("org_id", "default")
    run_id = configurable.get("run_id")
    if user_id:
        return UserContext(user_id=user_id, org_id=org_id, run_id=run_id)
    return None
```

### 任务 0.3：数据库 schema 确认（A 写，B 确认）

两人一起过一遍所有表的字段，确认无异议后 A 去写 Alembic 迁移。

表清单：

```
users           — id, email, password_hash, display_name, avatar_url, locale, is_active, created_at, updated_at
sessions        — id, user_id, token, expires_at, created_at
threads         — id, user_id, org_id, title, status, agent_name, default_model, last_model_name, created_at, updated_at, last_active_at
thread_runs     — id, thread_id, user_id, org_id, model_name, agent_name, sandbox_id, status, started_at, finished_at, error_message
user_memory     — id, user_id, org_id, context_json, updated_at
user_memory_facts — id, user_id, memory_id, content, category, confidence, source, created_at
user_souls      — id, user_id, org_id, content, updated_at
user_mcp_configs — id, user_id, org_id, config_json, updated_at
user_agents     — id, user_id, org_id, name, description, model, tool_groups_json, soul_content, created_at, updated_at
user_api_keys   — id, user_id, org_id, provider, api_key_enc, base_url, is_active, created_at, updated_at
```

### 任务 0.4：约定确认

```
runtime.context 字段：
  x-user-id, x-org-id, run_id, thread_id, model_name, agent_name,
  thinking_enabled, is_plan_mode, subagent_enabled

Redis key：
  session:{token}       → AuthContext JSON, TTL 5min
  run:{run_id}:key      → {api_key, base_url} JSON, TTL 5min
  rate_limit:{user_id}  → 计数器, TTL 60s

文件路径（本地开发时 base_dir = backend/.deer-flow）：
  {base_dir}/users/{user_id}/threads/{thread_id}/user-data/workspace/
  {base_dir}/users/{user_id}/threads/{thread_id}/user-data/uploads/
  {base_dir}/users/{user_id}/threads/{thread_id}/user-data/outputs/
  {base_dir}/users/{user_id}/threads/{thread_id}/user-data/tmp/
  {base_dir}/users/{user_id}/skills/custom/{skill_name}/
```

### 任务 0.5：提交契约代码

```bash
git add backend/packages/harness/deerflow/stores.py
git add backend/packages/harness/deerflow/context.py
git commit -m "feat: define cross-layer interface contracts for multi-tenant"
```

---

## 第二步：A 和 B 各自开工（并行）

契约提交后，两人可以完全独立工作。

---

### A 的任务清单

按顺序做，每个任务完成后单独提交。

#### A-1. Alembic 初始化

```
新建 backend/alembic.ini
新建 backend/alembic/env.py
生成 001_baseline.py（从现有 models.py）
修改 app.py 移除 Base.metadata.create_all

验证：cd backend && PYTHONPATH=. uv run alembic upgrade head
```

#### A-2. Auth 数据库表

```
新建 002_auth_tables.py（users + sessions 表）
新建 models.py 中 User, Session 模型

验证：alembic upgrade head 建表成功
```

#### A-3. Redis 客户端

```
新建 backend/app/gateway/redis_client.py
  - get_redis() → async Redis 连接
  - close_redis_pool()
修改 app.py lifespan 注册 shutdown hook
新增 pyproject.toml 依赖：redis[hiredis]>=5.0

验证：Gateway 启动时能连上本地 Redis
```

#### A-4. Gateway Auth API

```
新建 backend/app/gateway/routers/auth.py
  - POST /api/auth/register
  - POST /api/auth/login
  - POST /api/auth/logout
  - GET  /api/auth/session
  - GET  /api/auth/check
重写 backend/app/gateway/auth.py 的 get_auth_context
注册时自动创建个人 Organization + membership

验证：
  curl -X POST localhost:8001/api/auth/register -d '{"email":"test@test.com","password":"test1234","display_name":"Test"}'
  curl -c cookies.txt -X POST localhost:8001/api/auth/login -d '{"email":"test@test.com","password":"test1234"}'
  curl -b cookies.txt localhost:8001/api/auth/session
  curl -b cookies.txt localhost:8001/api/auth/check  # 看响应头 X-User-Id
```

#### A-5. 用户 API

```
新建 backend/app/gateway/routers/users.py
  - GET /api/users/me
  - PUT /api/users/me

验证：curl -b cookies.txt localhost:8001/api/users/me
```

#### A-6. 前端 Auth 改造

```
删除 frontend/src/server/better-auth/ 目录
删除 frontend/src/app/api/auth/[...all]/route.ts
新建 frontend/src/core/auth/api.ts
修改 login/page.tsx → 调 Gateway API
修改 register/page.tsx → 调 Gateway API
修改 middleware.ts

验证：浏览器注册 → 登录 → 进入 workspace → 登出
```

#### A-7. Thread 业务表 + API

```
新建 004_threads.py 迁移（threads + thread_runs 表）
新建 backend/app/gateway/routers/threads.py
  - POST   /api/threads
  - GET    /api/threads
  - PATCH  /api/threads/{id}
  - DELETE /api/threads/{id}
  - POST   /api/threads/{id}/runs
  - PATCH  /api/threads/{id}/runs/{run_id}
Gateway 内部用 httpx.AsyncClient 调 LangGraph:2024

验证：curl 创建 thread → 列出 → 删除
```

#### A-8. 前端 Thread Hooks 改造

```
新建 frontend/src/core/threads/threads-api.ts
修改 frontend/src/core/threads/hooks.ts
  - useThreads → 调 Gateway
  - useDeleteThread → 调 Gateway
  - useRenameThread → 调 Gateway
  - sendMessage → submit 前加 Gateway 创建 thread + thread_run
  - onUpdateEvent → title 变化时调 Gateway PATCH
  - onFinish → 更新 thread_run 状态

验证：浏览器新建对话 → 发消息 → sidebar 显示 → 删除
```

#### A-9. Memory / Soul / MCP 表 + PG Store 实现

```
新建 005_user_data.py 迁移
新建 services/memory_store_pg.py
新建 services/soul_store_pg.py
新建 services/skill_config_store_pg.py
新建 services/mcp_config_store_pg.py
修改 routers/memory.py, soul.py, mcp.py, agents.py, skills.py — 按 user_id 查询

验证：单元测试 CRUD
```

---

### B 的任务清单

按顺序做，每个任务完成后单独提交。

#### B-1. 文件路径重构

```
修改 backend/packages/harness/deerflow/config/paths.py
  - user_dir(user_id)
  - user_thread_dir(user_id, thread_id)
  - user_thread_tmp_dir(user_id, thread_id)
  - user_skills_dir(user_id)

验证：单元测试，路径生成正确
```

#### B-2. Skills Loader 改造

```
修改 backend/packages/harness/deerflow/skills/loader.py
  - load_skills(user_id=None)
  - 有 user_id → public + users/{user_id}/custom + SkillConfigStore 开关
  - 无 user_id → 回退到现有全局行为

验证：
  - 无 user_id：行为和现在完全一样（make dev 不受影响）
  - 有 user_id：加载对应目录
```

#### B-3. Memory 接口对接

```
修改 backend/packages/harness/deerflow/memory/updater.py
  - MemoryUpdater 接受 MemoryStore 参数
  - 不再直接读写 JSON 文件
  - 无 MemoryStore 时回退到 JSON 文件（兼容本地开发）

验证：传入 mock MemoryStore 能正常工作
```

#### B-4. MCP 工具加载 per-user

```
修改 backend/packages/harness/deerflow/mcp/cache.py
  - get_cached_mcp_tools(user_id=None)
  - 有 user_id → 平台默认 + McpConfigStore 覆盖
  - 缓存 key 从全局改为 (user_id, config_mtime)

验证：不同 user_id 加载不同 MCP 配置
```

#### B-5. Soul / Memory 加载注入 prompt

```
修改 prompt 构建逻辑
  - 从 SoulStore.get_soul(user_id) 读取 soul
  - 从 MemoryStore.get_memory(user_id) 读取 memory
  - 注入到 system prompt

验证：生成的 prompt 包含正确的 per-user soul 和 memory
```

#### B-6. make_lead_agent 改造

```
修改 backend/packages/harness/deerflow/agents/lead_agent/agent.py
  - 从 config["configurable"] 提取 user_id（通过 get_user_context）
  - 传给 load_skills / load_memory / load_soul / get_mcp_tools
  - 从 ModelKeyResolver.resolve_key(run_id) 读取 API Key
  - 无 user_id 时回退到现有全局行为

验证：
  - make dev 正常工作（无 user_id 回退）
  - 传入 user_id 时加载对应数据
```

#### B-7. ThreadData Middleware 改造

```
修改 backend/packages/harness/deerflow/agents/middleware/thread_data.py
  - 从 runtime.context 读取 user_id
  - 创建 per-user 目录结构
  - 新增 thread 私有 tmp 目录

验证：目录创建在 users/{user_id}/threads/{thread_id}/ 下
```

#### B-8. 虚拟路径映射改造

```
修改 sandbox/tools.py 中的路径解析
  - /mnt/user-data/* → users/{user_id}/threads/{thread_id}/...
  - /mnt/skills/public/* → skills/public/...
  - /mnt/skills/custom/* → users/{user_id}/skills/custom/...
  - 禁止跨用户路径访问

验证：路径翻译正确，跨用户访问被拒绝
```

#### B-9. Store 注册机制

```
实现一个简单的 store registry / 依赖注入
  - harness 启动时从外部注入 store 实现
  - make_lead_agent 从 registry 获取 store
  - 未注入时使用本地文件回退

验证：注入 mock store 后能正确读写
```

#### B-10. Checkpointer 切换到 PostgreSQL

```
修改 checkpointer/async_provider.py
  - 确保 postgres 类型读取 CHECKPOINT_POSTGRES_URI 环境变量
修改 config.yaml checkpointer 配置

验证：LangGraph 启动时连接本地 PG checkpointer
```

---

## 联调检查点

### 检查点 1：A-4 + B-1 完成后

```
A 的 auth 能工作 + B 的路径重构完成
→ 确认两人代码不冲突
→ 合并一次
```

### 检查点 2：A-7 + B-6 完成后（关键联调）

```
A 的 Thread API 就绪 + B 的 make_lead_agent 改造完成
→ A 把 PG Store 实现注入 B 的 harness
→ 联调完整链路：
  1. 浏览器注册登录
  2. 创建 thread（走 Gateway）
  3. 发消息（走 LangGraph，带 user_id）
  4. harness 按 user_id 加载数据
  5. 流式返回
  6. sidebar 显示 thread
```

### 检查点 3：A-9 + B-9 完成后

```
所有 per-user 数据隔离就绪
→ 两个用户登录，互相看不到对方数据
→ 全面回归测试
```

---

## 本地开发注意事项

1. `make dev` 要继续能用
   - B 的所有改动必须加 `if user_id` 分支
   - 无 user_id 时回退到现有行为
   - 这样本地开发不受影响

2. nginx auth_request 本地怎么测
   - 本地开发时 nginx 用 `docker/nginx/nginx.local.conf`
   - 需要在这个文件里加 auth_request 配置
   - 或者本地测试时先跳过 auth_request，用 header 手动注入 X-User-Id

3. 本地 PG 和 Redis
   - 用 brew 安装，不用 Docker
   - 开发时数据库名 `allo`，无密码
   - Redis 无密码

4. 两人代码冲突面
   - A 改 `backend/app/gateway/` 和 `frontend/`
   - B 改 `backend/packages/harness/deerflow/`
   - 唯一可能冲突的文件：`backend/app/gateway/app.py`（注册 router）
   - 建议：A 负责 app.py 的修改，B 不动这个文件
