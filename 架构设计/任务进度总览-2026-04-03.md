# 任务进度总览 — 2026-04-03

> 基于 `架构设计/生产部署方案.md` 的 7 Phase 计划，对照当前实际完成状态。

---

## Phase 1: 基础设施 ✅ 100%

| 子任务 | 状态 | 说明 |
|--------|------|------|
| 1.1 服务器基础环境 | ✅ | Ubuntu 22.04, PG16, Redis, nginx, Python3.12, Node22, pnpm, Docker, uv |
| 1.2 Alembic 迁移体系 | ✅ | 5 个 migration (001→002→004→005→006)，服务器已 `upgrade head` |
| 1.3 Redis 客户端模块 | ✅ | `redis_client.py` 完成，session 缓存 + rate limit 已接入 |
| 1.4 应用进程管理 | ✅ | systemd 三个服务已在服务器运行（gateway/langgraph/frontend） |
| 1.5 Checkpointer 切换 PG | ✅ | `async_provider.py` 支持 PG checkpointer |
| 1.6 依赖更新 | ✅ | alembic, redis, asyncpg, bcrypt 等已加入 |
| nginx 配置 | ✅ | 本地 + 生产配置，auth_request 已验证 |
| env 模板 | ✅ | `deploy/env/allo.env.production.example` |
| 服务器部署 | ✅ | 三个服务 active，外部 http://146.56.239.94 可访问 |

---

## Phase 2: 认证 & 用户系统 ✅ 95%

| 子任务 | 状态 | 说明 |
|--------|------|------|
| 2.1 Gateway Auth 实现 | ✅ | register/login/logout/session/check 全部完成 |
| 2.2 数据库 Schema | ✅ | users, sessions, organizations, organization_members |
| 2.3 Session 验证中间件 | ✅ | Redis 缓存 + PG fallback，`get_auth_context` |
| 2.4 注册自动创建 Org | ✅ | 每用户独立 org |
| 2.5 前端改造 | ✅ | 移除 Better Auth，login/register 页面调 Gateway API |
| 2.6 用户 API | ⚠️ 80% | `GET /api/users/me` 有，`PUT /api/users/me` 待完善 |
| 2.7 给现有表补 user_id | ✅ | knowledge_bases, tenant_configs 等已有 user_id |
| Cookie 安全加固 | ✅ | HttpOnly + SameSite=lax + 生产 Secure |
| 注册防刷 | ✅ | per-IP Redis 限流 5/hour |
| 本地 dev 账号 | ✅ | 固定 ID seed + 自动 install marketplace skills |

---

## Phase 3: 数据隔离（Harness 层） ✅ 90%

| 子任务 | 状态 | 说明 |
|--------|------|------|
| 3.1 用户上下文传播 | ✅ | `context.py` + nginx header + thread fallback |
| 3.1.1 Gateway↔LangGraph 边界 | ✅ | SDK 流式 + Gateway CRUD，前端 hooks.ts 改造完成 |
| 3.2 Memory 迁移 PG | ✅ | `PostgresMemoryStore` + 空结构修复 + debounce 写入验证通过 |
| 3.3 MCP 配置 Per-User | ⚠️ 80% | PG store 有，`get_cached_mcp_tools` 传 user_id 已接通，API 层待完善 |
| 3.4 Skills Per-User | ✅ 90% | filesystem 发现 + marketplace gating + user toggle + custom 目录全链路接通 |
| 3.4.1 Skill 路径映射 | ✅ | 虚拟路径 + 跨用户防穿越 |
| 3.5 Custom Agents Per-User | ✅ | `user_agents` 表 + CRUD 按 user_id 过滤 |
| 3.5.1 Soul Per-User | ⚠️ 70% | PG store 有，runtime 预解析已接入，API 端点待验证 |
| 3.6 Threads 业务表 | ✅ | threads + thread_runs 表，ownership 双键校验 |
| 3.7 Thread 文件隔离 | ✅ | `users/{user_id}/threads/{thread_id}/` 路径 |
| 线程越权防护 | ✅ | `_enforce_thread_ownership()` 在 LangGraph runtime 层校验 |

---

## Phase 4: Marketplace & Skill 运行时 ✅ 90%

| 子任务 | 状态 | 说明 |
|--------|------|------|
| Marketplace CRUD | ✅ | skills + tools 浏览/安装/卸载 |
| Skill Catalog Resolver | ✅ | filesystem + marketplace gating + user toggle + marketplace-only skills |
| Tool Gating | ✅ | 预解析到 metadata，避免 async loop 错误 |
| Seed 幂等化 | ✅ | `merge()` + 模块级标志 + 测试不泄漏 |
| 前端 Marketplace 页面 | ✅ | 安装/卸载 UI |
| 前端 Settings Skills tab | ✅ | Public + Custom + Marketplace 三个 tab |

---

## Phase 5: 安全加固 ✅ 85%

| 子任务 | 状态 | 说明 |
|--------|------|------|
| Thread 越权访问 | ✅ | `_enforce_thread_ownership()` |
| Memory 隔离 | ✅ | 按 user_id 查询 |
| Skill toggle 隔离 | ✅ | 按 user_id + org_id |
| Marketplace install 隔离 | ✅ | 按 org_id |
| Session Cookie 安全 | ✅ | HttpOnly + SameSite + Secure(prod) |
| 注册防刷 | ✅ | per-IP 5/hour |
| Sandbox 路径防穿越 | ✅ | 跨用户路径检测 |
| Rate Limiting | ✅ | per-org 令牌桶 |
| HTTPS/TLS | ❌ | 等域名就绪 |
| CSRF 防护 | ⚠️ | SameSite=lax 提供基本防护，无独立 CSRF token |

---

## Phase 6: 生产部署 ⚠️ 70%

| 子任务 | 状态 | 说明 |
|--------|------|------|
| 代码推送到服务器 | ✅ | GitHub → gitclone.com 镜像 → /srv/allo |
| 后端依赖安装 | ✅ | `uv sync` + 清华 PyPI 镜像 |
| 前端构建 | ✅ | `pnpm build` 成功 |
| DB 迁移 | ✅ | `alembic upgrade head` 23 张表 |
| systemd 服务启动 | ✅ | 三个服务 active |
| nginx 配置 | ✅ | HTTP 模式，auth_request 生效 |
| 用户注册验证 | ✅ | alice/bob 注册成功 |
| Thread 隔离验证 | ✅ | Alice 访问 Bob thread → 404 |
| config.yaml 配置 | ❌ | 模型未配置，发消息会报错 |
| HTTPS | ❌ | 等域名 |
| systemd enable | ❌ | 服务未设为开机自启 |
| 备份策略 | ❌ | 脚本已有，cron 未配 |

---

## Phase 7: Sandbox Docker Per-User ❌ 0%

| 子任务 | 状态 | 说明 |
|--------|------|------|
| Docker sandbox pool | ❌ | 代码层面路径隔离已有，Docker 容器管理未实现 |
| 资源限制 | ❌ | CPU/内存/磁盘限制未配 |
| 空闲超时回收 | ❌ | 未实现 |

---

## 本次会话解决的问题汇总

| # | 问题 | 类型 | 提交 |
|---|------|------|------|
| 1 | history/resume user_id=None | Bug | aad425b |
| 2 | marketplace tool gating "different loop" | Bug | aad425b |
| 3 | Memory update KeyError 'user' | Bug | aad425b |
| 4 | Memory 页面 RangeError 崩溃 | Bug | aad425b |
| 5 | Marketplace skill 安装后不显示 | Feature | aad425b |
| 6 | Marketplace seed 重复插入 | Bug | a48e610 |
| 7 | 测试数据泄漏到生产 DB | Bug | ca6f662 |
| 8 | 本地 dev 账号每次重启丢状态 | Bug | 2f06f5f → d9df5f0 |
| 9 | LangGraph 线程越权访问 | Security | ff997d7 |
| 10 | Session Cookie 缺少安全标志 | Security | ff997d7 |
| 11 | 注册接口无防刷 | Security | ff997d7 |
| 12 | 服务器部署（PG/systemd/nginx） | Deploy | 服务器直接操作 |

---

## 下一步优先级

### 立即要做（才能真正跑通发消息）
1. **服务器 config.yaml 配置模型** — 当前 `config.yaml` 是 example 模板，没有配置任何 chat model，发消息会报 `No chat models are configured`
2. **systemd enable** — `sudo systemctl enable allo-gateway allo-langgraph allo-frontend`

### 短期（1-2 天）
3. Soul per-user API 端点验证
4. MCP per-user API 完善
5. 用户 profile 更新 API
6. 备份 cron 配置

### 中期（域名就绪后）
7. HTTPS/TLS (certbot)
8. Cookie secure=true 生效

### 后续
9. Sandbox Docker per-user pool
10. 新用户注册自动 install 免费 marketplace skills
