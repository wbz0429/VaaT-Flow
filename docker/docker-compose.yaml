# Allo Appliance — Self-contained Docker deployment
# Usage: docker compose up -d
#
# Services:
#   - postgres:    PostgreSQL 16 database (persistent via pgdata volume)
#   - nginx:       Reverse proxy (port 2026, configurable via PORT env var)
#   - frontend:    Next.js production server
#   - gateway:     FastAPI Gateway API
#   - langgraph:   LangGraph production server
#   - provisioner: (optional) Sandbox provisioner for Kubernetes mode
#
# Key environment variables:
#   POSTGRES_PASSWORD                — database password, default deerflow_secret
#   BETTER_AUTH_SECRET               — optional in appliance mode, generated by install.sh
#   DEER_FLOW_DOCKER_SOCKET          — optional Docker socket path for advanced sandbox installs
#
# Data is stored in named volumes (pgdata, appdata). Config files are embedded
# in the image and copied to appdata on first boot if not already present.
#
# LangSmith tracing is disabled by default (LANGCHAIN_TRACING_V2=false).
# Set LANGCHAIN_TRACING_V2=true and LANGSMITH_API_KEY in environment to enable.
#
# Access: http://localhost:${PORT:-2026}

services:
  # ── Database ────────────────────────────────────────────────────────────────
  postgres:
    image: postgres:16-alpine
    container_name: deer-flow-postgres
    environment:
      POSTGRES_DB: deerflow
      POSTGRES_USER: deerflow
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-deerflow_secret}
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks:
      - deer-flow
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U deerflow"]
      interval: 5s
      timeout: 3s
      retries: 10

  # ── Reverse Proxy ──────────────────────────────────────────────────────────
  nginx:
    image: nginx:alpine
    container_name: deer-flow-nginx
    ports:
      - "${PORT:-2026}:2026"
    volumes:
      - ./nginx/${NGINX_CONF:-nginx.conf}:/etc/nginx/nginx.conf:ro
    depends_on:
      frontend:
        condition: service_started
      gateway:
        condition: service_healthy
      langgraph:
        condition: service_healthy
    networks:
      - deer-flow
    restart: unless-stopped

  # ── Frontend: Next.js Production ───────────────────────────────────────────
  frontend:
    build:
      context: ../
      dockerfile: frontend/Dockerfile
      target: prod
      args:
        PNPM_STORE_PATH: ${PNPM_STORE_PATH:-/root/.local/share/pnpm/store}
    container_name: deer-flow-frontend
    environment:
      - BETTER_AUTH_SECRET=${BETTER_AUTH_SECRET:-appliance-local-secret}
      - DATABASE_URL=postgresql://deerflow:${POSTGRES_PASSWORD:-deerflow_secret}@postgres:5432/deerflow
      - ALLO_MODE=appliance
    networks:
      - deer-flow
    restart: unless-stopped

  # ── Gateway API ────────────────────────────────────────────────────────────
  gateway:
    build:
      context: ../
      dockerfile: backend/Dockerfile
    container_name: deer-flow-gateway
    command: sh -c "mkdir -p /app/data/.deer-flow && [ -f /app/data/config.yaml ] || cp /app/config.yaml /app/data/config.yaml && [ -f /app/data/extensions_config.json ] || cp /app/extensions_config.json /app/data/extensions_config.json && cd backend && PYTHONPATH=. uv run uvicorn app.gateway.app:app --host 0.0.0.0 --port 8001 --workers 2"
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - appdata:/app/data
      # DooD: AioSandboxProvider starts sandbox containers via host Docker daemon
      - ${DEER_FLOW_DOCKER_SOCKET:-/var/run/docker.sock}:/var/run/docker.sock
    working_dir: /app
    environment:
      - CI=true
      - ALLO_MODE=appliance
      - PLATFORM_MODEL_API_KEY=${PLATFORM_MODEL_API_KEY:-}
      - PLATFORM_MODEL_BASE_URL=${PLATFORM_MODEL_BASE_URL:-}
      - DATABASE_URL=postgresql+asyncpg://deerflow:${POSTGRES_PASSWORD:-deerflow_secret}@postgres:5432/deerflow
      - DEER_FLOW_HOME=/app/data/.deer-flow
      - DEER_FLOW_CONFIG_PATH=/app/data/config.yaml
      - DEER_FLOW_EXTENSIONS_CONFIG_PATH=/app/data/extensions_config.json
      # DooD path/network translation
      - DEER_FLOW_HOST_BASE_DIR=/app/data/.deer-flow
      # In appliance images, packaged skills live at /app/skills. Override only for
      # advanced source-checkout / Docker-socket installs that need a host path.
      - DEER_FLOW_HOST_SKILLS_PATH=${DEER_FLOW_HOST_SKILLS_PATH:-/app/skills}
      - DEER_FLOW_SANDBOX_HOST=host.docker.internal
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - deer-flow
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/health"]
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 10s

  # ── LangGraph Server ───────────────────────────────────────────────────────
  # TODO: switch to langchain/langgraph-api (licensed) once a license key is available.
  # For now, use `langgraph dev` (no license required) with the standard backend image.
  langgraph:
    build:
      context: ../
      dockerfile: backend/Dockerfile
    container_name: deer-flow-langgraph
    command: sh -c "mkdir -p /app/data/.deer-flow && [ -f /app/data/config.yaml ] || cp /app/config.yaml /app/data/config.yaml && [ -f /app/data/extensions_config.json ] || cp /app/extensions_config.json /app/data/extensions_config.json && cd /app/backend && uv run langgraph dev --no-browser --allow-blocking --no-reload --n-jobs-per-worker 4 --host 0.0.0.0 --port 2024"
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - appdata:/app/data
      # DooD: same as gateway
      - ${DEER_FLOW_DOCKER_SOCKET:-/var/run/docker.sock}:/var/run/docker.sock
    environment:
      - CI=true
      - ALLO_MODE=appliance
      - PLATFORM_MODEL_API_KEY=${PLATFORM_MODEL_API_KEY:-}
      - PLATFORM_MODEL_BASE_URL=${PLATFORM_MODEL_BASE_URL:-}
      - DATABASE_URL=postgresql+asyncpg://deerflow:${POSTGRES_PASSWORD:-deerflow_secret}@postgres:5432/deerflow
      - DEER_FLOW_HOME=/app/data/.deer-flow
      - DEER_FLOW_CONFIG_PATH=/app/data/config.yaml
      - DEER_FLOW_EXTENSIONS_CONFIG_PATH=/app/data/extensions_config.json
      - DEER_FLOW_HOST_BASE_DIR=/app/data/.deer-flow
      # In appliance images, packaged skills live at /app/skills. Override only for
      # advanced source-checkout / Docker-socket installs that need a host path.
      - DEER_FLOW_HOST_SKILLS_PATH=${DEER_FLOW_HOST_SKILLS_PATH:-/app/skills}
      - DEER_FLOW_SANDBOX_HOST=host.docker.internal
      # Disable LangSmith tracing — LANGSMITH_API_KEY is not required.
      # Set LANGCHAIN_TRACING_V2=true and LANGSMITH_API_KEY in environment to enable.
      - LANGCHAIN_TRACING_V2=${LANGCHAIN_TRACING_V2:-false}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - deer-flow
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:2024/ok"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 30s

  # ── Sandbox Provisioner (optional, Kubernetes mode) ────────────────────────
  provisioner:
    profiles:
      - provisioner
    build:
      context: ./provisioner
      dockerfile: Dockerfile
    container_name: deer-flow-provisioner
    volumes:
      - ~/.kube/config:/root/.kube/config:ro
    environment:
      - K8S_NAMESPACE=deer-flow
      - SANDBOX_IMAGE=enterprise-public-cn-beijing.cr.volces.com/vefaas-public/all-in-one-sandbox:latest
      - SKILLS_HOST_PATH=${DEER_FLOW_HOST_SKILLS_PATH:-/app/skills}
      - THREADS_HOST_PATH=/app/data/.deer-flow/threads
      - KUBECONFIG_PATH=/root/.kube/config
      - NODE_HOST=host.docker.internal
      - K8S_API_SERVER=https://host.docker.internal:26443
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - deer-flow
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8002/health"]
      interval: 10s
      timeout: 5s
      retries: 6

networks:
  deer-flow:
    driver: bridge

volumes:
  pgdata:
  appdata:
